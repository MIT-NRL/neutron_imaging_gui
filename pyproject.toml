[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "neutron-imaging-gui"
version = "0.2.0"
description = "Interactive neutron-imaging data reduction workflow."
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Sean Fayfar", email = "sfayfar@mit.edu" }]
requires-python = ">=3.10"
dependencies = [
    "matplotlib",
    "numpy",
    "pyqtgraph",
    "qtpy",
    "threadpoolctl",
    "tifffile",
    "neutron-imaging-tools",
]

[project.optional-dependencies]
dev = ["ruff"]
tomography = ["tomopy"]

[project.scripts]
neutron-imaging-gui = "neutron_imaging_gui.__main__:main"

[project.urls]
Homepage = "https://github.com/MIT-NRL/neutron_imaging_gui"
Repository = "https://github.com/MIT-NRL/neutron_imaging_gui"
Issues = "https://github.com/MIT-NRL/neutron_imaging_gui/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py310"
